$kind: http-request
method: GET
url: '{{baseUrl}}/api/organizations/slug/:slug'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
pathVariables:
  - key: slug
    value: my-organization
