$kind: http-request
method: DELETE
url: '{{baseUrl}}/api/organizations/{{organizationId}}'
order: 13000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
pathVariables:
  - key: organizationId
    value: '{{organizationId}}'
