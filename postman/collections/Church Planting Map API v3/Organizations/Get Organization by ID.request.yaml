$kind: http-request
method: GET
url: '{{baseUrl}}/api/organizations/{{organizationId}}'
order: 2000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
pathVariables:
  - key: organizationId
    value: '{{organizationId}}'
