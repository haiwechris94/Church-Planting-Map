$kind: http-request
method: GET
url: '{{baseUrl}}/api/analytics/regions'
order: 5000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
    disabled: true
queryParams:
  - key: country
    value: '{{countryCode}}'
    disabled: true
