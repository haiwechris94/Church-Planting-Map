$kind: http-request
method: GET
url: '{{baseUrl}}/api/voronoi/statistics'
order: 6000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
    disabled: true
queryParams:
  - key: country
    value: '{{countryCode}}'
    disabled: true
